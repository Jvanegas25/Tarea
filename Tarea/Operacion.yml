info:
  name: Operacion
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:5000/operacion
  body:
    type: json
    data: |-
      {
        "a":10,
        "b":5,
        "operacion": "division"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
